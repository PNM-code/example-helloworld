name: test push

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  test-hello-world:
    name: Test Hello World Programs
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
